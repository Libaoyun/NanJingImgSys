<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="PhotoPropertyExMapper">
    <!--namespace根据自己需要创建的的mapper的路径和名称填写-->
    <sql id="Base_Column">
        id, serial_num as serialNum, property_name as propertyName, property_content as propertyContent,
        proptery_type as propteryType, property_type_code as propertyTypeCode, img_serial_num as imgSerialNum,
        body_part_serial_num as bodyPartSerialNum, create_by as createBy, create_time as createTime,
        modified_by as modifiedBy, modified_time as modifiedTime
    </sql>

    <select id="queryBySerialNum" parameterType="pd" resultType="pd">
        select
        <include refid="Base_Column"/>
        from imgsys_img_property_ex
        where serial_num = #{serialNum}
    </select>

    <select id="queryByImgSerialNumList" parameterType="java.util.List" resultType="pd">
        select
        <include refid="Base_Column"/>
        from imgsys_img_property_ex
        where img_serial_num IN
        <foreach collection="list" item="item" separator="," open="(" close=")">
            #{item}
        </foreach>
        ORDER BY id ASC;
    </select>

</mapper>